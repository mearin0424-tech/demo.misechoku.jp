# TEST-SCENARIOS.md — ミセチョク シナリオテストケース

外部テスター向けのシナリオテストケース集。**テスターは自分でアカウントを新規作成**するところから
開始します。既存のシード（`cast01@test.jp` 等）は基本的に使わず、
**自分だけの Cast / Shop アカウント**を作って全フローを通します。

- 対応 URL: `https://demo.misechoku.jp`（本番相当のステージング）
- 想定端末: **スマートフォン Chrome / Safari を第一優先**、次点で PC Chrome
- Bug 報告は所定フォーマット（末尾 §14）で提出
- 優先度: **P0=必須（リリースブロッカー）** / **P1=重要** / **P2=推奨**

---

## §0. デモ環境の特別動作（Mock ヘルパー）

外部テスターが**メール受信・LINE OAuth を用意せずに**全機能を確認できるよう、
デモ環境（`.env` の `DEMO_MODE=true`）では以下の Mock が有効化されています。

| ヘルパー | 動作 |
|---|---|
| **メール認証の即時承認** | 「認証メール送信」ボタンを押すと、実際のメール送信をスキップし、`email_verified_at` が即座に更新される。「[デモ環境] メール送信をスキップし … 即時認証済みにしました。」のトーストが出れば OK |
| **モック LINE ログイン** | `/login/line/mock` に「[デモ] モック LINE でログイン」ボタン。任意の「モック LINE ユーザー ID」を入力するとその ID で認証コールバックがシミュレートされる。連携済みでない ID を使うとログイン画面に戻る |
| **モック LINE 連携** | ログイン後、`/setting/notification` の「[デモ] モック LINE 連携」フォームで、任意の ID（未入力なら自動生成）を現在のアカウントに紐付ける |
| **テスト Push 送信** | `/setting/notification` の「[デモ] テスト Push を送る」ボタン。事前に「通知を有効化」で端末登録が必要 |

これらのボタンは `DEMO_MODE=true` の環境でのみ表示されます。もし見当たらない場合は運営に環境設定を確認してください。

**ID プレフィックスの注意**: モック LINE ID は内部的に `mock:` プレフィックスが付与されます。
連携時に「mock-abc123」を入れて連携した場合、次回モックログインでも同じ「mock-abc123」を入力してください
（`mock:` は入力不要）。

---

## §1. 事前準備

### 1.1 テスト実施前チェック
- [ ] `https://demo.misechoku.jp/welcome` が 200 で表示される
- [ ] `/login/demo` にアクセスできる
- [ ] `/login/demo` で「[デモ] モック LINE でログイン」ボタンが見える（DEMO_MODE 有効の証拠）
- [ ] スマホ Chrome で `https://demo.misechoku.jp/manifest.json` が JSON を返す

### 1.2 テスターが用意するもの
- 実在のメールアドレス（DEMO_MODE で自動承認されるためテスト時は届かない前提。本番では受信確認が必要）
- スマートフォン（Android Chrome / iOS Safari）
- 任意のプロフィール画像 3〜10 枚（本人ではないダミー可）
- 本人確認書類の**代替画像**（免許証を模した画像素材でも可、実際の個人情報は使わない）
- 店舗許可証の**代替画像**（風営 / 深夜酒類の代替画像。実データ不要）

### 1.3 テストで作成するアカウント
テスターは 1 セッション内で以下 4 種類のアカウントを自身で作成します。
すべて「テスト氏名 + 連番」など識別しやすいニックネームで登録してください。

| # | 種別 | 用途 |
|---|---|---|
| A | キャスト | メインの求職者ペルソナ |
| B | 店舗（オーナー） | メインの求人ペルソナ |
| C | 店舗のスタッフ | オーナーが招待するサブログイン |
| D | 追加キャスト（任意） | ブロック相手・スカウト上限テスト用 |

管理者は既存の `admin@test.jp / password` を使用してください（新規作成不可）。

### 1.4 ブラウザ / OS 網羅
- Android Chrome（最新）
- iOS Safari（最新）
- macOS / Windows Chrome（最新）
- スクリーン幅: 375px（iPhone SE 相当）と 1280px（デスクトップ）の両方

---

## §2. アカウント作成・認証 [TC-A]

### TC-A01 キャスト新規登録（P0）
**前提**: 未ログイン
**手順**:
1. `/welcome` からキャスト登録動線に進む（または直接 `/cast/register`）
2. メールアドレス（例: `tester+cast01@example.com`）・パスワード・生年月日・ニックネーム等を入力
3. 「登録する」を送信
4. 登録直後、`/cast/tutorial` または同等のオンボーディング画面に着地
5. チュートリアルを完了

**期待結果**:
- バリデーション正常で登録完了
- 生年月日は実在日付のみ受理（例: `2000/02/30` で 422）
- パスワード確認一致必須
- 同一メールで再登録すると 422（`email` unique）
- ホーム `/cast/home` に到達できる

**注意**: このアカウントを **アカウント A** として、以降のキャスト向けシナリオで使い続けます。

### TC-A02 メール認証（DEMO_MODE 自動承認）（P0）
**前提**: アカウント A でログイン中。メール認証未完了バナーが表示されている
**手順**:
1. バナー、もしくは `/setting/notification` の該当箇所から「認証メールを送信」相当のボタンを押す
2. 画面上のメッセージを確認

**期待結果**:
- 「[デモ環境] メール送信をスキップし、`tester+cast01@example.com` を即時認証済みにしました。」トースト
- ページを再読み込みするとメール未認証バナーが消える
- （本番環境シミュレーション: `.env` を一時的に `DEMO_AUTO_VERIFY_EMAIL=false` にした場合は、
  実際に署名 URL を含むメールが送信されるはず。テスターは通常 DEMO_MODE 有効前提でよい）

### TC-A03 店舗（オーナー）新規登録（P0）
**手順**:
1. `/shop/register` にアクセス
2. 店舗名・メールアドレス・パスワード・業態（`business_type`）・プランを入力
3. 送信 → `/shop/tutorial` に着地
4. TC-A02 と同様にメール認証を実施

**期待結果**:
- `business_type` / `plan` の値が in 値以外なら 422
- ホーム `/shop/home` に到達できる

**注意**: このアカウントを **アカウント B** として、以降の店舗向けシナリオで使い続けます。

### TC-A04 パスワードリセット（P1）
**手順**:
1. ログアウト → `/password/forgot`
2. アカウント A のメールアドレスを入力 → 送信
3. **DEMO_MODE 環境ではメール送信もモック化されていない場合**、リセットリンクは
   `storage/logs/laravel.log` に記録される想定。実運用テスターは運営に確認するか、下記を参照
4. 存在しないメールを入れても同じ完了文言が返ることを確認（enumeration 対策）

**期待結果**:
- 実在 / 非実在で同じ文言（enumeration 防止）
- リンクは 1 時間で失効
- 変更後は新パスワードでログイン可能

> 補足: パスワードリセットはメール送信必須。テスト環境が SMTP を持たない場合、代替として運営が発行するトークンを利用する。

### TC-A05 モック LINE 連携 → モック LINE ログイン（P0）
**前提**: アカウント A でログイン中
**手順**:
1. `/setting/notification` を開く
2. 画面下部の「[デモ] モック LINE 連携」フォームに「`tester-line-a`」と入力して「連携」
3. 「[デモ] モック LINE を連携しました。LINE User ID = tester-line-a …」トーストを確認
4. ログアウト → `/login/demo`
5. 「キャストで入る」を選択 → 「[デモ] モック LINE でログイン」をクリック
6. モックフォームで role=`キャストで入る` / user_id=`tester-line-a` を入力し送信

**期待結果**:
- 手順 3 で連携が完了
- 手順 6 でアカウント A に再ログインでき、`/cast/home` に着地
- 連携していない ID（例: `not-linked-xxx`）でログインを試みると、`/cast/login` に戻り「このモック LINE ID … に紐づくアカウントがありません」旨のエラー

### TC-A06 店舗のモック LINE 連携・ログイン（P1）
**手順**: TC-A05 と同じ流れをアカウント B（店舗）で実施（`role=店舗で入る` / user_id=`tester-line-b`）

**期待結果**: 店舗ホーム `/shop/home` に着地

### TC-A07 ログイン失敗のレート制限（P1）
**手順**: `/cast/login` に誤ったパスワードで **6 回** 連続送信

**期待結果**: 6 回目でロック（`throttle:5,15` = 15 分に 5 回）

### TC-A08 停止中アカウント（P1）
**前提**: 管理者（`admin@test.jp`）で `/admin/casts` からアカウント A を「停止」
**手順**: 別ブラウザで アカウント A にログインを試みる

**期待結果**: `/account/suspended` に強制リダイレクト。通常機能不可
**後片付け**: 管理者から「停止解除」

---

## §3. キャスト機能 [TC-C]

### TC-C01 プロフィール完成（P0）
**前提**: アカウント A でログイン
**手順**: `/cast/profile/edit`
- 画像を 3 枚アップロード → 並び替え → 1 枚削除
- 性別 / 年齢層 / エリア / 業態経験を入力
- 相性診断（personality_type、例 `INTJ`）を実施

**期待結果**:
- 画像 0 枚保存で 422
- 電話番号や URL を含む本文で 422（NG ワード検出）
- 相性診断は 4 文字（`INTJ` など）以外を拒否

### TC-C02 本人確認書類提出（P0）
**手順**: `/cast/mypage/identity`
- 代替画像を表面 / 裏面としてアップロード（8MB 以下 JPG/PNG）
- 「審査を依頼する」

**期待結果**:
- 状態が「審査中」に変化
- **原本 URL を直接 GET しても 404**（private disk からストリーム配信のみ）

### TC-C03 銀行口座登録（P0）
**手順**: `/cast/mypage` → 口座登録
- 銀行 4 桁・支店 3 桁・口座番号 7〜8 桁を `/api/bank-lookup` の候補から選択
- 口座名義は全角カナで入力

**期待結果**:
- 半角カナ / ひらがなの口座名義は 422
- 銀行検索 API がリアルタイムに候補を返す

### TC-C04 「今すぐ入れる」宣言（P1）
**手順**: `/cast/mypage` → 「今すぐ入れる」ボタン → 2h / 4h / 8h → 取消

**期待結果**:
- 直後、店舗ホームの Tier A 上位にアカウント A が並ぶ（アカウント B で確認）
- 取消後は Tier A から外れる

### TC-C05 SWIPE ホーム（P0）
**手順**: `/cast/home`
- カードを左右スワイプ（NOPE / LIKE / SUPER）
- 5 件以上さばく

**期待結果**:
- ダーク画面のまま（`body` に `theme-light` が付かない）
- SUPER LIKE は星アイコン発火

### TC-C06 店舗検索（P0）
**手順**: `/cast/search/list`
- キーワード + エリア + 業態フィルタを 2 つ以上組み合わせ
- 並び替えを「マッチ度（relevance）」に切替
- 気になる店舗を KEEP → `/cast/search/keep` タブに遷移して表示確認

**期待結果**:
- Premium 契約中の店舗（テスターが後述の TC-P で契約するアカウント B）が上位グループに固定表示
- 期限切れ Premium は上位に来ない
- KEEP 済みだけが `keep` タブに出る

### TC-C07 AI レコメンド（P1）
**手順**: `/cast/search/ai` で自然文入力（例「渋谷でランチ短時間」）

**期待結果**:
- 数秒以内にレコメンド結果が返る
- ゼロ件時も代替提案が表示される（無応答にならない）

### TC-C08 店舗プロフィール詳細（P0）
**手順**: 検索結果から店舗詳細 → 概要 / 求人票 / レビュー / KEEP → 「トークを始める」

**期待結果**:
- プレミアムホワイト表示
- KEEP トグルが動作（DevTools ネットワークタブで `/api/favorites/toggle` 200）
- 閲覧が `profile_views` に記録される（管理画面で TC-M で確認）

---

## §4. 店舗機能 [TC-S]

### TC-S01 店舗プロフィール編集（オーナー）（P0）
**前提**: アカウント B でログイン
**手順**: `/shop/profile/edit`
- 住所 / 電話 / 営業時間 / 画像を編集
- 「駅を提案」で近隣駅の候補が出る（`/api/geocoding/*`）

**期待結果**: 保存後、店舗詳細に反映

### TC-S02 求人票編集（オーナー）（P0）
**手順**: `/shop/recruits/edit`
- 時給 / ボーナス / タグ / キャッチコピー / マネージャーメッセージを編集
- 「AI で提案」（キャッチコピー・マネージャーメッセージ）を数回叩く
- 「公開 / 非公開」トグル

**期待結果**:
- 時給 min:0 未満で 422
- 存在しないタグ ID で 422
- トグル切替で検索結果に即反映

### TC-S03 店舗スタッフ追加 → スタッフでログイン（P0）
**前提**: アカウント B（オーナー）でログイン
**手順**:
1. `/shop/mypage/staff` → 「スタッフを追加」
2. メール（例: `tester+shopstaff@example.com`）・パスワードを入力 → 作成（**アカウント C**）
3. 別ブラウザでアカウント C にログインして `/shop/home` に到達
4. アカウント C から `/shop/profile/edit` を URL 直叩き

**期待結果**:
- 手順 3 で `/shop/home` に着地
- 手順 4 で 403（`shop.owner` ミドルウェア）
- 1 店舗 2 人目のオーナー作成は不可

### TC-S04 店舗許可証提出（オーナー限定・2 段階）（P0）
**手順**: `/shop/mypage/documents`
- 代替画像を風営 or 深夜酒類でアップロード
- 「審査を依頼」→ 途中で「取下げ」→ 再度「依頼」
- アカウント C（スタッフ）で同 URL を開く

**期待結果**:
- upload → request-review → withdraw のステータス遷移
- スタッフは 403、閲覧のみ可能

### TC-S05 キャスト検索（店舗側）（P0）
**手順**: `/shop/search`
- キーワード・タグ・エリアで絞り込み
- キャスト詳細 `/shop/castprofileview/{id}` を開く

**期待結果**:
- 検索プリファレンスが保存される
- キャスト詳細はプレミアムホワイト

### TC-S06 「今すぐ入れる」宣言（店舗）（P1）
**手順**: `/shop/mypage` → 「今すぐ入れる」宣言 → 2h/4h/8h → 取消

**期待結果**: キャストホームで Tier A に上がる（アカウント A で確認）

---

## §5. トーク・面談・ボーナス [TC-T / TC-B]

### TC-T01 トーク開始（アカウント A → B）（P0）
**手順**:
1. アカウント A から アカウント B の店舗プロフィールを開く
2. 「トークを始める」→ 応募（`shop_job_applications` 作成）
3. トークルームでメッセージ送信

**期待結果**: アカウント B のトーク一覧に着弾。両側で会話成立

### TC-T02 メッセージ削除（P0）
**手順**:
1. アカウント A で送信 → 10 分以内に自分のメッセージを削除
2. アカウント B から アカウント A のメッセージを削除しようと試みる

**期待結果**: 自分 + 10 分以内のみ削除可、他人メッセージは 403

### TC-T03 電話番号 / URL / LINE ID の検出（P0）
**手順**: 本文に以下を含めて送信
- `090-1234-5678`
- `line: hoge_id`
- `https://example.com`
- 管理画面で登録した NG ワード

**期待結果**: いずれも NG ワード検出で拒否

### TC-T04 面談オファー〜受諾（P0）
**手順**:
1. アカウント B（店舗）が `interview_offer` で候補 3 日時提示
2. アカウント A（キャスト）が 1 つ選んで `interview_confirm`

**期待結果**:
- 双方に「面談確定」バッジ
- 権限違反（キャストが `interview_offer` / 店舗が `interview_confirm`）は 403

### TC-T05 面談キャンセル（P1）
**手順**: アカウント B が `interview_cancel_request` → アカウント A が `interview_cancel_accept`

**期待結果**: ステータスがキャンセルに戻る

### TC-T06 採用 / 不採用（P0）
**手順**: アカウント B が `hired` または `rejected` を発火

**期待結果**: 応募ステータスに反映

### TC-T07 業務完了 / ボーナス達成報告（P1）
**手順**: アカウント A が `work_complete_report` / `bonus_achievement_report` を送信

**期待結果**: アカウント B の「採用・入金管理」に反映

### TC-T08 業務区分の合意（P2）
**手順**: 双方が `set_job_kind` で業務区分を合意

**期待結果**: どちら側からでも変更可能、履歴に残る

### TC-T09 ブロック（P0）
**手順**: アカウント A のトークルームメニューから アカウント B をブロック → 送信を試みる

**期待結果**: ブロック後の新規メッセージが送れない。解除は自分がブロックした側からのみ可能

### TC-B01 採用ボーナス完全フロー（Cast Request → Completed）（P0）
**前提**: TC-T06 で `hired` になった応募がある
**手順**:
1. **アカウント A**: `/cast/mypage/management` から「ボーナス請求」→ status=1
2. **アカウント B**: `/shop/mypage/management` から「請求を承認」→ status=2
3. **管理者**: `/admin/deposits` から該当行の「請求書発行」→ status=3
   - 請求書番号が `INV-YYYYMM-{4桁}` 形式
   - 期限が発行日 + 7 日
4. **アカウント B**: 「入金を申告」（振込済みと想定）→ status=4
5. **管理者**: 「入金確認済」→ status=5
6. **管理者**: 「キャストへ振込実行」→ status=6
7. **アカウント A**: 入金確認 → 「完了」→ status=7

**期待結果**: 全ステップでステータスバッジ・履歴が期待通り遷移

### TC-B02 二重請求の排除（P1）
**手順**: 同一応募 ID で 2 回目の請求

**期待結果**: 2 回目はエラー / 既存レコードを返す

### TC-B03 請求書 / 領収書 DL（P1）
**手順**: 管理画面から請求書 PDF DL、双方の画面で領収書 DL

**期待結果**: dompdf 導入時 PDF、未導入時 HTML

### TC-B04 レビュー投稿（P1）
**手順**: 完了後、双方がお互いにレビュー投稿
- NG ワード本文 / 星 0 / 星 6 も試す

**期待結果**: NG ワード 422、星は 1〜5 のみ

### TC-B05 店舗レビュー返信（P1）
**手順**: アカウント B が `/shop/mypage/reviews` から返信投稿・削除

**期待結果**: 自店舗のみ返信可、他店舗レビュー返信は 403

---

## §6. Premium プラン [TC-P]

### TC-P01 契約 → 入金確認 → 有効化（月払い）（P0）
**前提**: アカウント B（Premium 未加入）でログイン
**手順**:
1. `/subscription` を開く
2. 「月払いで契約する」
   - 金額 = 20,000 円
   - 請求書番号 = `PLN-YYYYMM-xxxx`
   - 期限 = 今日 + 7 日
3. 請求書 DL
4. 別ブラウザで管理者にログイン → `/admin/plans`
5. 該当行の「入金確認済」ボタン
6. アカウント B で `/subscription` を再読み込み → 「有効」表示
7. 領収書 DL

**期待結果**:
- 各画面で金額・期限・番号が一致
- 領収書番号は `RCT-YYYYMM-xxxx`
- Premium 機能（TC-P04 / TC-P05）が開放される

### TC-P02 契約 → 有効化（年払い）（P1）
**手順**: TC-P01 の流れで「年払い」を選択

**期待結果**:
- 金額 = 200,000 円
- `ends_at = 有効化日 + 1 年`

### TC-P03 契約キャンセル（P1）
**手順**:
1. アカウント B で月払い契約 → 入金待ち
2. 「申込みキャンセル」

**期待結果**:
- 入金待ちのみキャンセル可
- Active 状態はキャンセル不可

### TC-P04 スカウト送信上限（無料）（P0）
**前提**: アカウント B（Premium 未加入）でログイン。**アカウント D** をあらかじめ用意（TC-A01 と同手順で追加キャストを 6 名以上作成）
**手順**: 今日まだやりとりがない新規キャスト 6 人に順にメッセージ送信

**期待結果**:
- 5 人目までは送信成功
- 6 人目で 429（上限到達）

### TC-P05 スカウト送信上限（Premium）（P0）
**前提**: TC-P01 で Premium アクティブ化
**手順**: 新規キャスト 31 人に順に送信（既存やりとりのある相手は上限外）

**期待結果**:
- 30 人目まで成功、31 人目で 429
- 既にやりとりがある相手には無制限で送れる

### TC-P06 閲覧キャスト一覧（Premium 限定）（P1）
**手順**:
1. TC-P01 後のアカウント B（Premium）で `/shop/mypage/viewers` → 一覧表示
2. 別途 Premium 未加入の店舗を追加登録 → 同 URL でティーザー表示

**期待結果**:
- Premium: 閲覧者リスト + 最終閲覧日時
- 非 Premium: 「Premium で開放」旨のティーザーのみ

---

## §7. 管理画面 [TC-M]

**すべて `admin@test.jp / password` でログイン**

### TC-M01 ダッシュボード（P0）
**手順**: `/admin`

**期待結果**: サマリー数値が表示

### TC-M02 入金・振込管理（P0）
**手順**: `/admin/deposits`
- フィルタ・ソート
- 請求書 PDF DL / メール送付
- 「入金確認済」→「キャスト振込実行」
- 支払タスクの無効化・返金フラグ

### TC-M03 Premium 入金管理（P0）
**手順**: `/admin/plans`
- 入金待ちが上部に並ぶ
- TC-P01 の入金確認を実施

### TC-M04 本人確認 / 書類審査（P0）
**手順**: `/admin/verification`
- TC-C02 で提出したアカウント A の書類を承認 / 差戻し
- TC-S04 で提出したアカウント B の許可証を承認 / 差戻し
- 「purge」ボタンで完全削除

**期待結果**:
- 承認後、対象ユーザーの本人確認バッジが変化
- private disk からのファイル配信は署名 URL 経由のみ

### TC-M05 店舗 / キャスト管理（P1）
**手順**: `/admin/shops`, `/admin/casts`
- アカウント A / B を停止 → 対象アカウントで `/account/suspended` にリダイレクト
- 「解除」で通常アクセス可能
- 「非公開情報を閲覧」（パスワード再入力）

### TC-M06 マスタ・NG ワード管理（P1）
**手順**:
- `/admin/masters` から任意カタログを追加・並び替え・更新・削除
- `/admin/ngwords` で NG 語追加 → トーク送信で拒否確認 → 非アクティブ → 拒否されなくなる

### TC-M07 お知らせ / コラム / 通知仕様 / オコジョガイド（P1）
**手順**:
- `/admin/notices` お知らせ CRUD → `/support/notices` に反映
- `/admin/columns` コラム CRUD
- `/admin/notification-spec` で通知 ON/OFF・文言変更
- `/admin/character-guide` で任意画面を ON → 該当画面に反映

### TC-M08 通報・問合せ・規約管理（P1）
**手順**:
- `/admin/user-reports` 一覧・ステータス変更
- `/admin/support-inquiries` 一覧・ステータス変更・メモ
- `/admin/policies/{about,terms,privacy}` 編集 → ロック解除 → 保存

### TC-M09 運営アカウント管理（P1）
**手順**: `/admin/admin-accounts`
- staff ロールの権限を編集
- 操作ログを確認

**期待結果**: admin ロール（locked）は編集不可

### TC-M10 権限マトリクス（P0）
**前提**: staff ロールで一部権限のみ許可した状態
**手順**: staff アカウントで許可外のページを URL 直叩き

**期待結果**: 403 で拒否

---

## §8. 設定・退会 [TC-X]

### TC-X01 通知設定（P1）
**手順**: `/setting/notification` で各チャネル（Push / LINE / リマインダー）の ON/OFF 切替

**期待結果**: 切替が即時保存

### TC-X02 メール / パスワード変更（P0）
**手順**: `/setting/account`
- メール変更 → 再ログイン
- パスワード変更 → 旧不可・新可

### TC-X03 LINE 連携解除（P2）
**手順**: TC-A05 で連携済みのアカウント A で `/setting/account` から解除

**期待結果**: モック LINE ログインでは入れなくなる

### TC-X04 退会（P0）
**手順**: `/setting/account` → 「退会する」
- パスワード誤入力 → 拒否
- 正しいパスワード → 完了
- アカウント B（唯一のオーナー）で退会を試みる

**期待結果**:
- パスワード誤りは拒否
- 完了後、PII が匿名化される
- 最後のオーナーは退会不可（先にスタッフをオーナー昇格させるか閉店する必要）

---

## §9. 通知（アプリ内 / Push / メール / LINE）[TC-N]

**前提**: HTTPS で配信されていること（demo.misechoku.jp は HTTPS 前提）

ミセチョクの通知は 4 系統あります。テスターは**それぞれ**動作を確認してください。

| 系統 | 実体 | 到達先 | ON/OFF |
|---|---|---|---|
| **アプリ内通知** | `notifications` テーブル + ヘッダーのベルアイコン | ログイン中の画面上部 | 常時 ON（既読管理のみ） |
| **Web Push（PWA）** | `push_subscriptions` + `PushNotificationService` | ブラウザ / OS のシステム通知 | `/setting/notification` の「通知チャンネル（Push）」 |
| **メール** | Laravel Mail（面接リマインダー / 期限リマインダーなど） | 登録メール | `/setting/notification` の各リマインダー |
| **LINE 通知** | `LineNotificationService`（sendToCast / sendToShopManager） | LINE トーク | `/setting/notification` の「通知チャンネル（LINE）」+ LINE 連携済み必須 |

### TC-N01 PWA インストール（Android Chrome）（P1）
**手順**:
1. スマホ Chrome で `https://demo.misechoku.jp` を開く
2. 数秒操作した後、メニュー → 「アプリをインストール」または「ホーム画面に追加」
3. インストール後、ホーム画面のアイコンから起動

**期待結果**:
- インストール可能ダイアログが表示される
- 起動時はスタンドアロン（ブラウザ UI なし）
- タスクトレイ / 履歴では独立したアプリ扱い

**確認 URL**: `chrome://apps` に「ミセチョク」が並ぶ（デスクトップの場合）

### TC-N02 PWA インストール（iOS Safari）（P1）
**手順**:
1. iOS Safari で開く → 共有 → 「ホーム画面に追加」
2. アイコンから起動

**期待結果**:
- スタンドアロン起動
- iOS ではブラウザとは別アイコン
- **注意**: iOS はブラウザ自動プロンプトが出ない仕様（ユーザー操作が必要）

### TC-N03 Push 通知 端末登録（P0）
**前提**: アカウント A でログイン
**手順**:
1. `/setting/notification` を開く
2. 「通知を有効化」ボタンをクリック
3. ブラウザの通知許可プロンプトで「許可」

**期待結果**:
- 「この端末で通知を受け取れるようになりました。」トースト
- DB `push_subscriptions` にレコードが 1 件増える（管理者確認）
- ブロック / 拒否の場合は「通知がブロックされています。ブラウザ設定で許可してください。」

### TC-N04 [デモ] テスト Push 送信（P0）
**前提**: TC-N03 の端末登録が完了している
**手順**: `/setting/notification` の「[デモ] テスト Push を送る」→「テスト送信」

**期待結果**:
- 数秒以内に**端末（またはブラウザ）**にデスクトップ / モバイル通知が表示される
- 通知タイトル =「[デモ] テスト通知」
- 通知本文 =「ミセチョクのテスト通知です（HH:MM:SS）」
- PWA として起動中はアプリアイコンにバッジが付く（対応 OS）

### TC-N05 実イベントでの通知（P0）
**前提**: TC-N03 端末登録済み
**手順**:
1. アカウント A で `/setting/notification` を開き Push を有効化 & 「テスト」で疎通確認
2. 別ブラウザで アカウント B から アカウント A にメッセージ送信
3. 面談確定など、TC-T04 の各アクションを発火

**期待結果**:
- 実イベント（新着メッセージ / 面談確定 / 期限リマインダー / 入金確認 など）で Push 到達
- 通知タップでアプリ内該当画面に着地（`data.url`）

### TC-N06 バッジ数（P2）
**手順**:
1. 端末登録後、ヘッダーのベルアイコンに未読数バッジが出る
2. `/notifications/read-all` で全既読 → バッジが 0
3. PWA として起動中はアプリアイコンにも同期される

**期待結果**: `initBadgeFromPage()` により Service Worker 経由でバッジが同期

### TC-N07 端末拒否 / 削除（P2）
**手順**:
1. TC-N03 後、ブラウザ設定で通知を「ブロック」に変更
2. 再度「通知を有効化」→ エラー
3. サブスクリプションが失効した場合、`push_subscriptions` から自動削除される（管理者確認）

**期待結果**:
- ブロックすると新規通知が届かない
- 失効 subscription は自動削除（`WebPush::isSubscriptionExpired()`）

### TC-N08 通知 OFF 設定尊重（P1）
**手順**: `/setting/notification` で「通知チャンネル（Push）」を OFF に → 実イベントを発火

**期待結果**: OFF のカテゴリの Push は届かない（テスト Push は設定に関わらず届く点に注意）

### TC-N09 アプリ内通知（ベルアイコン）（P0）
**前提**: アカウント A（キャスト）でログイン
**手順**:
1. 別ブラウザで アカウント B から アカウント A へメッセージ送信
2. アカウント A の画面右上ベルアイコンの**未読バッジ数**が +1 されているか
3. ベルをタップ → 通知ポップアップが開く
4. 該当通知をタップ → トークルーム等の該当 URL に遷移
5. 「すべて既読」ボタンをタップ → バッジが 0 になる
6. `/notifications/unread-count` を DevTools で GET → JSON で `{count: 0}` が返る

**期待結果**:
- 通知ポップアップにタイトル + 本文 + 相対時刻が表示
- タップで既読化 + 遷移
- リロード後もバッジ数が維持される
- 空状態は「全ての通知を確認済みです。」

### TC-N10 通知タイプ別 — イベントごとの到達確認（P0）

各イベントで**アプリ内通知**と**Web Push**（有効化済みなら）の両方が届くことを確認します。
LINE 連携中なら LINE 通知も届く（TC-N12 で個別確認）。

| # | イベント | 起点 | 通知先 | 想定タイトル/本文 |
|---|---|---|---|---|
| 1 | 新着メッセージ | 相手が送信 | 自分 | 「〇〇さんから新着メッセージ」 |
| 2 | 面談オファー | 店舗が `interview_offer` | キャスト | 「面談日時の候補が届きました」 |
| 3 | 面談確定 | キャストが `interview_confirm` | 店舗 | 「面談日時が確定しました」 |
| 4 | 面談キャンセル | 店舗が `interview_cancel_request` | キャスト | 「面談キャンセルの依頼」 |
| 5 | 採用 | 店舗が `hired` | キャスト | 「採用されました」 |
| 6 | 不採用 | 店舗が `rejected` | キャスト | 「今回はご縁がありませんでした」 |
| 7 | 業務完了報告 | キャストが `work_complete_report` | 店舗 | 「業務完了の報告」 |
| 8 | ボーナス請求 | キャストが請求 | 店舗 | 「ボーナス請求が届きました」 |
| 9 | ボーナス承認 | 店舗が承認 | キャスト | 「請求が承認されました」 |
| 10 | 請求書発行 | 管理者が発行 | 店舗 | 「請求書が発行されました」 |
| 11 | 入金確認済 | 管理者が確認 | 店舗 | 「入金を確認しました」 |
| 12 | キャストへ振込完了 | 管理者が振込 | キャスト | 「振込が完了しました」 |
| 13 | 本人確認 承認 / 差戻し | 管理者操作 | キャスト | 「本人確認が承認されました / 差戻し理由」 |
| 14 | 店舗許可証 承認 / 差戻し | 管理者操作 | 店舗 | 「許可証が承認されました / 差戻し理由」 |
| 15 | Premium 契約有効化 | 管理者が入金確認 | 店舗 | 「Premium プランが有効になりました」 |
| 16 | 通報を受けた運営通知 | ユーザーが通報 | 管理者全員 | 「新規通報」 |
| 17 | 問い合わせを受けた運営通知 | ユーザーが送信 | 管理者全員 | 「新規問い合わせ」 |

**手順**: TC-A〜TC-P で作成したアカウント A / B / C を使って、上記 1〜17 を順に発火し、
それぞれ **対象ユーザーのベルアイコン（アプリ内通知）** に着弾するか確認する。
Push を有効化しているデバイスなら**同内容の Web Push** も届くこと。

**期待結果**:
- 通知タップで正しい URL に遷移する（例: 新着メッセージ通知 → 該当トークルーム）
- 通知本文がユーザーにとって意味のある文言になっている
- **相手側だけに届く**（自分が起点のイベントで自分に通知が来ない）

### TC-N11 リマインダー通知（cron / 手動）（P1）
**前提**: `admin@test.jp` でサーバ側 artisan コマンドを実行できる環境（運営に依頼可）
**手順**:
1. **面接前リマインダー**（24 時間前 / 3 時間前）
   - 24 時間後 or 3 時間後の面談を仕込む
   - サーバで `php artisan reminders:send-interview-deadline --dry-run` → 対象一覧に自分が含まれる
   - dry-run を外して実行 → アプリ内通知 + Push が届く
2. **未読トークリマインダー**
   - 相手からのメッセージを 24 時間以上未読で放置
   - `php artisan reminders:send-unread-talk --dry-run` → 対象に含まれる
   - dry-run を外して実行 → 通知が届く
3. **請求期限リマインダー**
   - 請求書の期限が 前日 / 当日 / 超過 になっている状態
   - `reminders:send-interview-deadline` 内の期限ロジックで通知（コマンド名は運営に確認）

**期待結果**:
- リマインダー種別ごとに `/setting/notification` の設定（`interview_reminder_enabled` / `deadline_reminder_enabled`）が尊重される
- OFF にしていたリマインダーは届かない
- dry-run では実送信されない

**注意**: 実運用では cron で定期実行される想定。テスト環境では artisan 手動実行で代替。

### TC-N12 LINE 通知（P1）
**前提**:
- アカウント A で TC-A05 の**モック LINE 連携**を実施済み
- `/setting/notification` で「通知チャンネル（LINE）」を ON

**手順**:
1. アカウント B からアカウント A にメッセージ送信
2. 面談オファー・採用など他イベントも発火

**期待結果**:
- **本番の LINE Messaging API 経由**の場合、連携済み LINE アカウントに実メッセージが届く
- **DEMO_MODE 環境で LINE Bot が未設定の場合**、`storage/logs/laravel.log` に送信リクエストが記録される
  （実際の LINE 到達は不可）
- 「通知チャンネル（LINE）」を OFF にしたら送信されない

**代替確認**:
- 管理画面 `/admin/notification-spec` で「LINE 通知 ON/OFF」を切り替え → 該当イベントで送信可否が変わる

### TC-N13 メール通知（P2）
**前提**: SMTP が設定された環境（DEMO_MODE でも `MAIL_MAILER=log` 等でログ出力を確認可）
**手順**:
1. パスワードリセット（TC-A04）→ メールが届くはず
2. 面談リマインダー（TC-N11-1）→ メール送信ロジックがあれば届く
3. `MAIL_MAILER=log` の場合、`storage/logs/laravel.log` に本文が記録される

**期待結果**:
- SMTP 設定時: 実際にメール受信
- log ドライバ時: ログにメール本文（Subject / To / Body）が出力

### TC-N14 管理者への通報 / 問い合わせ通知（P1）
**手順**:
1. アカウント A からアカウント B を **通報**（TC-R02）
2. アカウント A から **問い合わせフォーム**（TC-R01）を送信
3. 管理者（admin@test.jp）でログイン → ベルアイコンを確認
4. `/admin` ダッシュボードの「未処理」件数が増えているか

**期待結果**:
- 管理者全員に通知が飛ぶ（`createForAllAdmins`）
- `/admin/user-reports` / `/admin/support-inquiries` に新規行が表示

### TC-N15 通知仕様のカスタマイズ（P2）
**手順**: `admin@test.jp` で `/admin/notification-spec`
1. 任意の通知タイプの ON/OFF を切替
2. 通知本文（テンプレート）を編集
3. 該当イベントを発火して、変更後の文言で届くか確認

**期待結果**: 通知本文が管理画面の変更内容で送信される

---

## §10. サポート・通報 [TC-R]

### TC-R01 サポートフォーム（P1）
**手順**:
- `/support/form` から問合せ送信
- 連続 6 回で 429

**期待結果**: 5 回目まで成功、6 回目で 429。管理画面 `/admin/support-inquiries` に届く

### TC-R02 ユーザー通報（P1）
**手順**: トークルームまたはプロフィール画面から相手を通報
- 自分を通報 → 422
- 同じ相手を 2 回通報 → 排除
- 60 分 21 回で 429

**期待結果**: 上記通り

### TC-R03 コラム・お知らせ（P2）
**手順**: `/support/column` / `/support/notices` を未ログインで閲覧

**期待結果**: 200 で表示

---

## §11. UI / テーマ [TC-U]

### TC-U01 テーマ振り分け（P1）
**手順**: 各ページを開いて body クラスを検証（DevTools）

| URL | 期待クラス |
|---|---|
| `/cast/home` | `mode-dark` / `theme-light` なし / `theme-premium-white` なし |
| `/cast/search/list` | `theme-light` |
| `/cast/mypage` | `theme-premium-white` |
| `/shop/mypage` | `theme-premium-white` |
| `/cast/shopprofiles/{id}` | `theme-premium-white` |
| `/share/recruit/{id}`（未ログイン） | ダーク維持 |

### TC-U02 ライト / ダーク トグル（P1）
**手順**: ヘッダーのトグルで「ダーク強制」→ 全ページ回遊

**期待結果**:
- Cookie `theme_mode=dark` セット
- `theme-light` / `theme-premium-white` が外れて全ページダーク
- クロームは常時紫ダーク

### TC-U03 ヘッダー中央タイトル（P2）
**手順**: 深階層ページを開く（`/shop/mypage/documents`, `/setting/notification` など）

**期待結果**:
- ヘッダー中央に日本語タイトル
- ページ内に大 h1 見出しがない

### TC-U04 オコジョガイド表示（P2）
**手順**: `/admin/character-guide` で任意画面を ON → 該当画面

**期待結果**: 上部にキャラガイド、メッセージが編集どおり

### TC-U05 管理画面テーブル（スマホ）（P2）
**手順**: 375px 幅で `/admin/deposits`, `/admin/casts` 等

**期待結果**: 1 行 = 1 カード縦積み、操作ボタン全幅、フィルタチップ横スクロール

### TC-U06 グローバルトースト（P2）
**手順**: 各種保存・エラーを発火

**期待結果**: 濃色パネル `window.appToast`（`alert()` 不使用）

---

## §12. 探索的テスト（自由シナリオ）

以下は**自由に触ってバグを見つける**時間。優先度 P2 として実施。

- スマホと PC で同時に同アカウントを開き、片方の操作の反映
- 極端に長い文字列（プロフィール本文 10,000 文字など）
- 画像アップロード（大きい / 破損 / 非対応拡張子）
- 面談日時に過去日を指定
- ネットワーク切断 → 再接続時の挙動
- CSRF トークン期限切れ（長時間放置後の送信）
- 戻る / 進む / リロードで二重送信が起きないか
- iOS Safari のセーフエリア（ノッチ / ホームインジケータ）
- 画面回転（縦 ↔ 横）でレイアウト崩れ
- Push 通知拒否後の再有効化フロー
- PWA スタンドアロン中に外部 URL（LP）を踏んだ時の挙動

---

## §13. 実施チェックリスト（サマリ）

- [ ] §2 アカウント作成・認証（TC-A01〜A08）
- [ ] §3 キャスト機能（TC-C01〜C08）
- [ ] §4 店舗機能（TC-S01〜S06）
- [ ] §5 トーク・面談・ボーナス（TC-T01〜T09 + TC-B01〜B05）
- [ ] §6 Premium プラン（TC-P01〜P06）
- [ ] §7 管理画面（TC-M01〜M10）
- [ ] §8 設定・退会（TC-X01〜X04）
- [ ] §9 通知 全系統（TC-N01〜N15：PWA/Push、アプリ内、イベント別到達、リマインダー、LINE、メール、管理者通知、仕様カスタマイズ）
- [ ] §10 サポート・通報（TC-R01〜R03）
- [ ] §11 UI / テーマ（TC-U01〜U06）
- [ ] §12 探索的テスト（4 時間以上）

**目安工数**: フルパスで **1.5〜2 人日**（スマホ + PC）

---

## §14. Bug 報告フォーマット

```
【Bug ID】（テスターが連番。例: BUG-001）
【対象テストケース】（TC-XXX / 探索的テストなら「探索」）
【優先度】P0 / P1 / P2 / P3
【端末 / OS / ブラウザ】例: iPhone 15 / iOS 17.4 / Safari
【スクリーン幅】例: 390px
【アカウント】自分で作った識別子（例: cast=tester+cast01@example.com）
【URL】例: https://demo.misechoku.jp/cast/mypage/identity
【再現手順】1. ... 2. ... 3. ...
【期待結果】...
【実際の結果】...
【スクリーンショット / 動画】添付
【備考】DevTools コンソールログ、ネットワークタブのエラー等
```

### 優先度の目安
- **P0**: 業務が止まる（ログイン不可・決済停止・データ破壊）
- **P1**: 主要フローに支障（Premium 契約不可・面談成立不可・Push 到達しない）
- **P2**: UI 崩れ / 使い勝手の劣化 / エッジケースの誤動作
- **P3**: 誤字・文言修正など

---

## §15. トラブルシューティング（テスター用）

### メール認証されない
- DEMO_MODE 環境では「認証メール送信」で即座に承認されるはず
- 「[デモ環境] メール送信をスキップし…」トーストが出ないなら DEMO_MODE 未有効 → 運営に連絡

### モック LINE ログインで「紐づくアカウントがありません」
- 事前に該当ロールで登録 + `/setting/notification` の「[デモ] モック LINE 連携」で ID を紐付ける必要あり
- 連携時に入力した ID とログイン時の ID を完全一致させる（大文字小文字含む）

### 「通知を有効化」で失敗する
- HTTPS でアクセスしているか確認（`localhost` はデスクトップのみ）
- ブラウザ設定で通知が「ブロック」になっていないか
- `/api/push/vapid-public-key` が 503 を返す場合、サーバ側 VAPID 未設定 → 運営に連絡

### テスト Push が届かない
- 先に「通知を有効化」を実施したか
- OS 側の集中モード / おやすみモード等で抑制されていないか
- PWA として起動している場合、アプリアイコンにバッジ、ブラウザとして開いている場合はデスクトップ通知として表示

### 画像アップロードが 413（Payload Too Large）
- 8MB を超えるファイルは拒否
- iOS の HEIC は JPG/PNG 変換が必要な場合あり
